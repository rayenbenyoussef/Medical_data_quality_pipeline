# .env.input — template file, copy to .env and fill in your values
# DO NOT add real credentials to this file — it is committed to git

# Database Configuration
DB_TYPE=
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

# Schema Names
RAW_SCHEMA=
STG_SCHEMA=
MRT_SCHEMA=