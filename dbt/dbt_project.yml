name: 'medical_data_quality_pipeline'
version: '1.0.0'
config-version: 2

profile: 'medical_data_quality_pipeline'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  medical_data_quality_pipeline:
    staging:
      +materialized: view
      +schema: "{{ env_var('STG_SCHEMA') }}"
    marts:
      +materialized: table
      +schema: "{{ env_var('MRT_SCHEMA') }}"

